{
  "name": "soapee",
  "version": "1.0.0",
  "license": "AGPL-3.0-or-later",
  "private": true,
  "scripts": {
    "install:client": "yarn --cwd client-web install",
    "dev": "yarn --cwd client-web start",
    "build": "yarn --cwd client-web build",
    "lint": "yarn --cwd client-web lint"
  }
}
